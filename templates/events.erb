<% @events.each do |event| %>
<% plan = event[:plan].to_s != '' ? "【#{event[:plan].strip}】" : ''  %>
* <%= event[:period] %> <%= plan %> <%= event[:title] %> <%= event[:facility].join(',') %>

<% end %>
